language: java

# Version number
java:
  - openjdk7
  - openjdk8
  - oraclejdk8
  - openjdk9
  - oraclejdk9

runtime:
  nodePool: custom__x86_64__CentOS_7

build:
  pre_ci_boot:
    image_name: drydock/c7javall
    image_tag: master
    pull: true
    
  ci:
    - java -version
    - docker ps
  #  - docker build -t=test .
    - docker images
    - mvn --version    
    - ant -version 
    - gradle --version
    